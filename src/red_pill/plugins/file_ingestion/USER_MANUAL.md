# User Manual

To use this plugin, simply drop `.md`, `.txt`, or `.pdf` files into any folder specified in your `.env` under `INGESTION_DIRECTORIES` (default: `~/.agent/ingestion`).
The agent will automatically process and memorize them.
