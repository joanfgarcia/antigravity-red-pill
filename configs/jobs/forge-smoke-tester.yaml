# Forge — rol Smoke Tester como UNA etapa atómica del dag_job.
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: dag_job
priority: 5
mission_id: forge-smoke-tester
backend: opencode
timeout: 600
control:
  max_step_minutes: 15
manifest:
  workdir: .
  stages:
    - id: smoke-tester
      type: agent
      minion: agent
      backend: opencode
      model: flash  # SEED por-instalación. decisión por rol en DECISION_LOG.md (smoke-tester → opencode-go/mimo-v2.5).
      effort: medium
