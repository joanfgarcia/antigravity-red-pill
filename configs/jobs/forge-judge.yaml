# Forge — rol Judge (arbitra refutaciones en conflicto; L3).
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: agentic_job
priority: 5
backend: opencode
model: opencode-go/kimi-k2.7-code  # política NOTE_MODEL_POLICY_ROLES (judge — k3 reservado)
effort: high
timeout: 600
control:
  max_step_minutes: 15
