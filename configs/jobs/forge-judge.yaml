# Forge — rol Judge (arbitra refutaciones en conflicto; L3) como etapa atómica.
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: dag_job
priority: 5
mission_id: forge-judge
backend: opencode
timeout: 600
control:
  max_step_minutes: 15
manifest:
  workdir: .
  stages:
    - id: judge
      type: agent
      minion: agent
      backend: opencode
      model: flash  # SEED por-instalación. Ver Aleth_Core/NOTE_MODEL_POLICY_ROLES.md (judge → opencode-go/kimi-k2.7-code).
      effort: high
