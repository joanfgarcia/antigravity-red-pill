# Forge — rol QA Final (auditoría completa antes del gate).
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: agentic_job
priority: 5
backend: opencode
model: opencode-go/mimo-v2.5-pro  # política NOTE_MODEL_POLICY_ROLES (qa-final)
effort: high
timeout: 900
control:
  max_step_minutes: 20
