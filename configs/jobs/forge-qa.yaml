# Forge — rol QA Final (auditoría completa antes del gate).
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: agentic_job
priority: 5
backend: opencode
model: flash  # SEED por-instalación: cada instalación configura el modelo de este rol. "flash" = default del harness (el bridge no pasa -m). Ver la política de modelos de referencia en Aleth_Core/NOTE_MODEL_POLICY_ROLES.md (p.ej. implementor → opencode-go/deepseek-v4-pro).
effort: high
timeout: 900
control:
  max_step_minutes: 20
