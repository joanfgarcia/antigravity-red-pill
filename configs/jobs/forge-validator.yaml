# Forge — rol Validator como UNA etapa atómica del dag_job.
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: dag_job
priority: 5
mission_id: forge-validator
backend: opencode
timeout: 600
control:
  max_step_minutes: 15
manifest:
  workdir: .
  stages:
    - id: validator
      type: agent
      minion: agent
      backend: opencode
      model: flash  # SEED por-instalación. Ver Aleth_Core/NOTE_MODEL_POLICY_ROLES.md (validator → opencode-go/deepseek-v4-pro).
      effort: medium
