# Forge — rol Validator (verifica criterios de aceptación por ejecución real).
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: agentic_job
priority: 5
backend: opencode
model: opencode-go/deepseek-v4-pro  # política NOTE_MODEL_POLICY_ROLES (validator)
effort: medium
timeout: 600
control:
  max_step_minutes: 15
