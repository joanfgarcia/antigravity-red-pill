# Forge — rol Implementor (ejecuta UNA fase con evidencia real).
# source: agentic_job — el prompt con spec/criterios/contexto viaja dinámico
# en el payload del submit; este recipe fija el perfil del rol.
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: agentic_job
priority: 5
backend: opencode
model: opencode-go/deepseek-v4-pro  # política NOTE_MODEL_POLICY_ROLES (implementor)
effort: high
timeout: 900
control:
  max_step_minutes: 25
