# Forge/Scout — rol Scout Analyst (UNA lente de análisis; produce shards).
seed: true  # SEED genérico: plantilla por-instalación. Activar config real copiando a .red-pill/jobs/ (ver NOTE_MODEL_POLICY_ROLES).
source: agentic_job
priority: 5
backend: opencode
model: opencode-go/deepseek-v4-flash  # política NOTE_MODEL_POLICY_ROLES (scout)
effort: medium
timeout: 600
control:
  max_step_minutes: 15
